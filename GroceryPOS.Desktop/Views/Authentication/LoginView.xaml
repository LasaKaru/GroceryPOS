<UserControl x:Class="GroceryPOS.Desktop.Views.Authentication.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GroceryPOS.Desktop.Views.Authentication"
             xmlns:viewmodels="clr-namespace:GroceryPOS.Desktop.ViewModels.Authentication"
             xmlns:converters="clr-namespace:GroceryPOS.Desktop.Converters"
             d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0 0 0 20">
            <TextBlock Text="Welcome to GroceryPOS" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0 10 0 5"/>
            <TextBlock Text="Please log in to continue" FontSize="16" HorizontalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="0 10 0 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Content="Username:" Grid.Row="0" Grid.Column="0" Margin="5"/>
            <TextBox Grid.Row="0" Grid.Column="1" Width="200" HorizontalAlignment="Left"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

            <Label Content="Password:" Grid.Row="1" Grid.Column="0" Margin="5"/>
            <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Width="200" HorizontalAlignment="Left"
                         Margin="5"
                         PasswordChanged="PasswordBox_PasswordChanged"/>
        </Grid>

        <StackPanel Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0 10 0 10">
            <Button Content="Login" Width="150" Height="35" FontSize="16"
                    Command="{Binding LoginCommand}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
            </Button>
            <TextBlock Text="Logging in..." HorizontalAlignment="Center" Margin="0 10 0 0"
                       Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Center"
                   Text="{Binding ErrorMessage}" Foreground="Red" FontWeight="Bold" Margin="0 5 0 0"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
        <TextBlock Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Center"
                   Text="{Binding SuccessMessage}" Foreground="Green" FontWeight="Bold" Margin="0 5 0 0"
                   Visibility="{Binding SuccessMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <TextBlock Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0 20 0 0">
            <Hyperlink NavigateUri="#">Forgot Password?</Hyperlink> | <Hyperlink NavigateUri="#">Initial Setup (First Run)</Hyperlink>
        </TextBlock>
    </Grid>
</UserControl>
