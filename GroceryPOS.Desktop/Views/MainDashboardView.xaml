<UserControl x:Class="GroceryPOS.Desktop.Views.MainDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GroceryPOS.Desktop.Views"
             xmlns:viewmodels="clr-namespace:GroceryPOS.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:MainDashboardViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10 15"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0 5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#34495e"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1abc9c"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#283747">
            <StackPanel Margin="10">
                <TextBlock Text="GroceryPOS" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0 20 0 40" HorizontalAlignment="Center"/>

                <Button Content="Home" Style="{StaticResource SidebarButtonStyle}" Command="{Binding NavigateToHomeCommand}" />
                <Button Content="Products" Style="{StaticResource SidebarButtonStyle}" Command="{Binding NavigateToProductsCommand}" />
                <Button Content="Sales" Style="{StaticResource SidebarButtonStyle}" Command="{Binding NavigateToSalesCommand}" />
                <Button Content="Users (Admin)" Style="{StaticResource SidebarButtonStyle}" Command="{Binding NavigateToUsersCommand}" />

                <Border Height="1" Background="#4A6572" Margin="0 20"/>
                <Button Content="Logout" Style="{StaticResource SidebarButtonStyle}" Command="{Binding LogoutCommand}" />
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="#F0F2F5">
            <ContentControl Content="{Binding CurrentContent}" Margin="20"/>
        </Border>
    </Grid>
</UserControl>