<UserControl x:Class="GroceryPOS.Desktop.Views.InitialSetup.InitialSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GroceryPOS.Desktop.Views.InitialSetup"
             xmlns:viewmodels="clr-namespace:GroceryPOS.Desktop.ViewModels.InitialSetup"
             xmlns:converters="clr-namespace:GroceryPOS.Desktop.Converters"
             d:DataContext="{d:DesignInstance Type=viewmodels:InitialSetupViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" Margin="0 0 0 20">
            <TextBlock Text="Initial System Setup" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0 10 0 5"/>
            <TextBlock Text="Create your first Administrator account" FontSize="18" HorizontalAlignment="Center" Foreground="Gray"/>
        </StackPanel>

        <Grid Grid.Row="2" Grid.Column="1" Margin="0 10 0 10" Width="350">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Content="Username:" Grid.Row="0" Grid.Column="0" Margin="5"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

            <Label Content="Password:" Grid.Row="1" Grid.Column="0" Margin="5"/>
            <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="5"
                         PasswordChanged="PasswordBox_PasswordChanged"/>

            <Label Content="Confirm Password:" Grid.Row="2" Grid.Column="0" Margin="5"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="2" Grid.Column="1" Margin="5"
                         PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>

            <Label Content="First Name:" Grid.Row="3" Grid.Column="0" Margin="5"/>
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

            <Label Content="Last Name:" Grid.Row="4" Grid.Column="0" Margin="5"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
        </Grid>

        <StackPanel Grid.Row="3" Grid.Column="1" HorizontalAlignment="Center" Margin="0 10 0 10">
            <Button Content="Create Administrator" Width="200" Height="40" FontSize="18"
                    Command="{Binding CreateAdminCommand}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
            </Button>
            <TextBlock Text="Creating user..." HorizontalAlignment="Center" Margin="0 10 0 0"
                       Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top"
                   Text="{Binding ErrorMessage}" Foreground="Red" FontWeight="Bold" Margin="0 5 0 0"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
        <TextBlock Grid.Row="4" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top"
                   Text="{Binding SuccessMessage}" Foreground="Green" FontWeight="Bold" Margin="0 5 0 0"
                   Visibility="{Binding SuccessMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
    </Grid>
</UserControl>